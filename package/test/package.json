{
  "name": "proto-plugin-test",
  "type": "commonjs",
  "scripts": {
    "build": "npm run build:js && npm run build:wasm",
    "build:js": "node esbuild.mjs",
    "build:wasm": "extism-js dist/index.js -i src/index.d.ts -o dist/plugin.wasm",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@moonrepo/proto-pdk": ".."
  },
  "devDependencies": {
    "binaryen": "^117.0.0",
    "esbuild": "^0.20.2",
    "typescript": "^5.4.4"
  }
}
