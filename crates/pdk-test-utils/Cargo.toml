[package]
name = "proto_pdk_test_utils"
version = "0.4.0"
edition = "2021"
license = "MIT"
description = "Utilities for testing proto WASM plugins."
homepage = "https://moonrepo.dev/proto"
repository = "https://github.com/moonrepo/proto"

[dependencies]
proto_core = { version = "0.15.0", path = "../core" }
proto_pdk_api = { version = "0.5.0", path = "../pdk-api" }
proto_wasm_plugin = { version = "0.5.0", path = "../wasm-plugin" }
extism = { workspace = true }
serde_json = { workspace = true }
